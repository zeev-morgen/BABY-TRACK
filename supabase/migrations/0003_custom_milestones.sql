-- ============================================================================
-- אבני דרך משלכם
--
-- עד כה טבלת milestones החזיקה רק את 18 הפריטים הקבועים מהיומן המודפס,
-- והכותרת שלהם הגיעה מהקוד. עמודת label מאפשרת גם אבני דרך שההורים
-- מגדירים בעצמם: עבורן הכותרת נשמרת בשורה, ולפריטים הקבועים היא נשארת ריקה.
--
-- להרצה: SQL Editor -> הדבקה -> Run. בטוח להרצה חוזרת.
-- ============================================================================

alter table public.milestones
  add column if not exists label text not null default '';
